# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: deltaneutral-external-service
  namespace: default
spec:
  ports:
    - name: web
      port: 43080
      targetPort: 43080
      protocol: TCP
  type: ClusterIP

---
# endpoints.yaml
apiVersion: v1
kind: Endpoints
metadata:
  name: deltaneutral-external-service
  namespace: default
subsets:
  - addresses:
      - ip: 192.168.4.92
    ports:
      - name: web
        port: 43080
        protocol: TCP

---
# ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deltaneutral-ingress
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
    - host: deltaneutral.carter2099.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: deltaneutral-external-service
                port:
                  number: 43080
