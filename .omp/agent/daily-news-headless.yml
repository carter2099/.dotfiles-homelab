# Daily News headless-agent config.
# Keeps Codex-primary digest search and SearXNG fallback separate from other omp -p consumers.
advisor:
  enabled: false
  syncBacklog: off
providers:
  webSearchOrder:
    - codex
    - searxng
searxng:
  endpoint: http://localhost:8080
  categories: general,news
